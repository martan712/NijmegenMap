# Generated map assets under the Vite public root — rebuild with the
# datacollect raster stages (maps -> tiles -> finalize).
public/maps/
public/tiles/
public/maps.json

# Generated caches under public/data (the *.geojson there stay tracked):
#   illustration caches — rebuild with the images stage
public/data/images/
#   fetched story media (photos/docs/audio) — rebuild with the stories stage
public/data/stories/

# Node / Vite
node_modules/
dist/

# Python
__pycache__/
*.pyc

# Local editor / tool state
.claude/settings.local.json
.DS_Store

Notes
