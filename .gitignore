# Generated map assets under the Vite public root — rebuild with the
# datacollect raster stages (maps -> tiles -> finalize).
public/maps/
public/tiles/
public/maps.json

# Generated caches under public/data (the *.geojson there stay tracked):
#   flat media pool: images + Korfmacher drawings (data/images), audio
#   recordings (data/audio) — rebuild with: python -m datacollect media
public/data/images/
public/data/audio/

# Node / Vite
node_modules/
dist/

# Python
__pycache__/
*.pyc

# Local editor / tool state
.claude/settings.local.json
.DS_Store

Notes
