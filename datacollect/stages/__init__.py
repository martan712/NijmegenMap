"""Stages layer: the collection logic, one Stage (CLI subcommand) per concept.
Modules here are auto-discovered, so their @register_stage decorators run on
startup. Add a stage by dropping a module that defines and registers a Stage."""
