"""Narrative chapters. Each module declares one Chapter (a Story with its
threads, segments, map-states and blocks); auto-discovered, so adding a chapter
is just dropping a new file here. The `graph` stage emits each to its .ttl."""
