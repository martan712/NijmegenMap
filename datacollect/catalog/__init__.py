"""Catalog layer: declarative content (image sets, vector layers, story media,
the raster timeline). Auto-discovered, so registrations run on startup. This is
where the project grows — extend an existing list or drop a new module."""
