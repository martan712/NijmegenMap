"""
NijmegenKaart data-collection package.

A single tool that builds every locally-stored data artifact the frontend and
backend depend on, from the upstream open sources (Gemeente Nijmegen WMS/WFS,
Wikimedia Commons, Dutch Wikipedia, PDOK). Each concern is a stage; run one
stage or `all`. See `python -m datacollect --help`.
"""
